import { ArrowRight, ArrowUpRight, Compass } from 'lucide-react';
export function Welcome({ onSignIn, onSample, busy, error }: { onSignIn: () => void; onSample: () => void; busy: boolean; error: string }) {
  return <main className="welcome"><section className="hero"><div className="hero-copy"><p className="eyebrow"><span className="accent-dot" /> A journal for the decisions ahead</p><h1>You don’t need<br />all the answers.<br /><em>A way to learn.</em></h1><p className="hero-description">Think through a choice. Try something small.<br className="desktop-break" /> Come back to what happened — and take that<br className="desktop-break" /> experience into your next decision.</p><div className="hero-actions"><button className="large" onClick={onSignIn} disabled={busy}>{busy ? 'Opening Google sign-in…' : 'Start your decision journal'}<ArrowRight size={18} /></button><button className="text-button" onClick={onSample}>Explore a sample journey <ArrowUpRight size={16} /></button></div><p className="small muted">Sign in with Google. Your journal belongs to you.</p>{error && <p className="alert" role="alert">{error}</p>}</div>
    <button className="hero-notebook" onClick={onSample} aria-label="Explore the fictional project decision"><div className="notebook-top"><Compass size={22} /><span>FIELD NOTES / A FICTIONAL EXAMPLE</span><ArrowUpRight size={18} /></div><div className="notebook-title"><span className="eyebrow">A decision worth revisiting</span><h2>Build something bigger.<br />Or finish something useful?</h2></div><div className="comparison"><div><span className="eyebrow">I expected</span><p>Five students could find a study room without my help.</p><span className="handwritten">70% confident</span></div><div><span className="eyebrow">I discovered</span><p>Five did. Their feedback changed what I built next.</p><span className="handwritten">Smaller scope. Real feedback.</span></div></div><div className="notebook-footer"><span className="loop-mark">↳</span><p>Carry the learning forward.<br /><strong>What’s the smallest test for my next idea?</strong></p></div></button>
    </section><section className="process-strip" aria-label="How Foresight works">{[['01', 'Clarify the choice', 'Make your options and assumptions visible.'], ['02', 'Try something small', 'Record an expectation you can revisit.'], ['03', 'Learn from the outcome', 'Connect lessons to the evidence behind them.']].map(([n, title, copy]) => <div key={n}><span>{n}</span><div><h3>{title}</h3><p>{copy}</p></div></div>)}</section><section className="welcome-note"><p className="eyebrow">For projects, first jobs, and the choices in between</p><h2>A little less hindsight.<br />A little more <em>foresight.</em></h2><p>Gemini helps you ask better questions. You make the call.</p></section></main>;
}
