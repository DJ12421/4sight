import { ArrowRight, ArrowUpRight, Compass } from 'lucide-react';
export function Welcome({ onSignIn, onSample, busy, error }: { onSignIn: () => void; onSample: () => void; busy: boolean; error: string }) {
  return <main className="welcome"><section className="hero"><div className="hero-copy"><p className="eyebrow"><span className="accent-dot" /> Your private Gemini journal</p><h1>A place for thoughts<br />before they become<br /><em>your next move.</em></h1><p className="hero-description">Write what is happening. Reflect with Gemini.<br className="desktop-break" /> Follow the threads between pages and choices,<br className="desktop-break" /> then turn insight into something you can try.</p><div className="hero-actions"><button className="large" onClick={onSignIn} disabled={busy}>{busy ? 'Opening Google sign-in…' : 'Open your journal'}<ArrowRight size={18} /></button><button className="text-button" onClick={onSample}>Explore a sample journey <ArrowUpRight size={16} /></button></div><p className="small muted">Sign in with Google. Your writing belongs to you.</p>{error && <p className="alert" role="alert">{error}</p>}</div>
    <button className="hero-notebook" onClick={onSample} aria-label="Explore the fictional journal and decision journey"><div className="notebook-top"><Compass size={22} /><span>FIELD NOTES / A FICTIONAL EXAMPLE</span><ArrowUpRight size={18} /></div><div className="notebook-title"><span className="eyebrow">A thought worth following</span><h2>Build something bigger.<br />Or finish something useful?</h2></div><div className="comparison"><div><span className="eyebrow">I wrote</span><p>I keep worrying that a focused project will not look ambitious enough.</p><span className="handwritten">career · confidence</span></div><div><span className="eyebrow">I learned</span><p>A smaller test produced real feedback—and a clearer next decision.</p><span className="handwritten">One page became evidence.</span></div></div><div className="notebook-footer"><span className="loop-mark">↳</span><p>Carry the learning forward.<br /><strong>What’s the smallest test for my next idea?</strong></p></div></button>
    </section><section className="process-strip" aria-label="How Foresight works">{[['01', 'Write what is true', 'Capture a thought by typing or dictating it.'], ['02', 'Reflect and connect', 'Ask Gemini, add tags, and see explicit links grow.'], ['03', 'Try and learn', 'Turn insight into a decision you can revisit.']].map(([n, title, copy]) => <div key={n}><span>{n}</span><div><h3>{title}</h3><p>{copy}</p></div></div>)}</section><section className="welcome-note"><p className="eyebrow">For the thoughts, choices, and experiments in between</p><h2>A little less hindsight.<br />A little more <em>foresight.</em></h2><p>Gemini helps you reflect. Your evidence—and the decision—remain yours.</p></section></main>;
}
